[(Error (assertEqualToResult (ematch (kb) ($SS A_Each $X) ($SS A_Each $X)) ((SS-P_Each A_Each B_Each) (SS-Q_Each A_Each C_Each))) 
Expected: [(SS-P_Each A_Each B_Each), (SS-Q_Each A_Each C_Each)]
Got: [(ematch (SS-P_Each A_Each B_Each) ($SS A_Each $X) ($SS A_Each $X)), (ematch (SS-Q_Each A_Each C_Each) ($SS A_Each $X) ($SS A_Each $X))]
Missed result: (SS-P_Each A_Each B_Each))]
0.00user 0.00system 0:00.00elapsed 100%CPU (0avgtext+0avgdata 6312maxresident)k
0inputs+0outputs (0major+454minor)pagefaults 0swaps
