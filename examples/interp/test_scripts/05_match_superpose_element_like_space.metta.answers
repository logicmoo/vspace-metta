[(Error (assertEqualToResult (match (kb) ($SS A_Each $X) ($SS A_Each $X)) ((SS-P_Each A_Each B_Each) (SS-Q_Each A_Each C_Each))) 
Expected: [(SS-P_Each A_Each B_Each), (SS-Q_Each A_Each C_Each)]
Got: [(Error (match (SS-P_Each A_Each B_Each) ($SS A_Each $X) ($SS A_Each $X)) match expects a space as the first argument), (Error (match (SS-Q_Each A_Each C_Each) ($SS A_Each $X) ($SS A_Each $X)) match expects a space as the first argument)]
Missed result: (SS-P_Each A_Each B_Each))]
0.10user 0.01system 0:00.11elapsed 98%CPU (0avgtext+0avgdata 33248maxresident)k
768inputs+0outputs (4major+3486minor)pagefaults 0swaps
